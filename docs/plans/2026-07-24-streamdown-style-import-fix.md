# Streamdown 样式导入构建失败修复计划

## 1、现状确认

- [X] 1.1 从最新 `dev` 创建 `fix/streamdown-style-import` 分支。
- [X] 1.2 核对报错导入、`streamdown` 实际安装包内容与锁文件版本。
- [X] 1.3 通过 Git 历史确认合并后依赖接口变化及根因。

## 2、修复开发

- [X] 2.1 选择与当前 `streamdown` 版本匹配的最小样式导入方案：按锁文件同步依赖。
- [X] 2.2 同步本地前端依赖，并确认 `streamdown/styles.css` 可解析。
- [X] 2.3 修正合并后发现的前端导入排序检查错误。

## 3、验证与交付

- [X] 3.1 运行前端静态检查、生产构建与页面访问验证。
- [X] 3.2 复核最终变更范围并完成 Review 记录。

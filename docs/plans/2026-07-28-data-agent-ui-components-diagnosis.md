# DataAgent 标签与审批组件不显示排查计划

## 1. 确认排查基线

- [X] 1.1 确认分支与工作区状态，避免把历史改动混进本次诊断。
- [X] 1.2 阅读仓库根目录与前端/后端 AGENTS 指南，锁定排查边界。
- [X] 1.3 阅读 DataAgent 数据流文档与相关实现入口。

## 2. 追踪后端数据链路

- [X] 2.1 核对 `publish_query_labels` 是否生成规范 `ToolMessage.artifact`。
- [X] 2.2 核对 `data_query_labels` custom event 是否被正确发出并序列化到流。
- [X] 2.3 核对 checkpoint / history 是否保留标签与审批 artifact。

## 3. 追踪前端渲染链路

- [X] 3.1 核对流式消息与历史消息的 artifact 解析逻辑。
- [X] 3.2 核对消息分组是否会生成 `assistant:query-intent`。
- [X] 3.3 核对 `QueryIntentCard` 与审批交互的渲染条件。

## 4. 验证根因

- [X] 4.1 运行前端单元测试，验证规范消息可以被渲染。
- [X] 4.2 运行后端回归与最小复现，验证后端可以产出规范消息。
- [X] 4.3 用本地数据库核对真实运行数据是否落到目标合同。

## 5. 输出结论

- [X] 5.1 记录直接根因与触发条件。
- [X] 5.2 记录最小修复建议与后续验证点。

## 6. 本轮重新调试与修复

- [X] 6.1 定位实体抽取、SQL 子任务内部 JSON 被前端兜底渲染的问题。
- [X] 6.2 收敛 `publish_query_labels` 模型可见参数，移除无业务作用且易抖动的 `confidence`。
- [X] 6.3 调整确认策略，不再依赖模型自报置信度，改为依赖服务端可验证的检索、绑定、标签和歧义状态。
- [X] 6.4 修正 `QueryIntentCard` 的内部枚举与 TableRAG Evidence 原始列表展示。
- [X] 6.5 为内部协议隐藏、标签工具 schema 和意图卡展示补充回归测试。
